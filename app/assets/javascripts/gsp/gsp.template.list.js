/**
 * GSP - Template List
 * Manage template deploy recurrence and owner settings
 *
 * Functionalities
 * 
 * - Quick filter list
 * - Hide/Show full template description
 * - Edit template deploy settings
 * +- Change recurrence schedule
 * +- Review owner
 * 
 * 
 */
 
 
 
(function(gsp, window, undefined) {

  function Item($DIV) {
    
  }

  gsp.TemplateList = function () {
    
  };
  
  // Unobstructively prepare DOM elements
  $(document).ready(function() {  GSP.TemplateList();  });
}(GSP, window));
