<div class="panel panel-reviews">
  <table id="task_list">
    <tr>
      <th>Completed Reviews</th>
      <th>Completion Date</th>
    </tr>
    
    <% @completed_reviews.each do |review| %>
    <tr>
      <td><%= link_to review.name, list_tasks_path(review.id) %></td>
      <td><%= Time.now.to_formatted_s(:review_date) %></td>
    </tr>
    <% end %>
    
    
  </table>
  
  <div>
    <p>Period: 1 year previous - Today</p>
    <!--p>Completed and in conformance</p>
    <p>Not completed but all tasks in conformance</p>
    <p>At least one review item in non-conformance</p-->
  </div>
</div>
