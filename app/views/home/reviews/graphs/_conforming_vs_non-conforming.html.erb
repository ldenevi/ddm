<!--Load the AJAX API-->
<script type="text/javascript" src="https://www.google.com/jsapi"></script>
<script type="text/javascript">

  // Load the Visualization API and the piechart package.
  google.load('visualization', '1.0', {'packages':['corechart']});

  // Set a callback to run when the Google Visualization API is loaded.
  google.setOnLoadCallback(drawChart);

  // Callback that creates and populates a data table,
  // instantiates the pie chart, passes in the data and
  // draws it.
  function drawChart() {

    // Create the data table.
    var data = new google.visualization.DataTable();
    data.addColumn('string', 'Status');
    data.addColumn('number', 'Count');
    data.addRows([
      ['Conforming', 16],
      ['Non-Conforming', 4]
    ]);

    // Set chart options
    var options = {'title':'Conforming vs. Non-Conforming Reviews',
                   'width':400,
                   'height':200,
                   'backgroundColor': 'transparent',
                   'slices' : [{ color: '#007C43' }, { color: '#FF000D' }] ,
                   is3D : true};

    // Instantiate and draw our chart, passing in some options.
    var chart = new google.visualization.PieChart(document.getElementById('non_conforming_graph'));
    chart.draw(data, options);
  }
</script>

<div id="non_conforming_graph">
</div>
